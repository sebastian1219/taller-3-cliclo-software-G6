permissions:
  actions: read
  contents: read
  security-events: write

name: Security Pipeline

on:
  push:
    branches: [ "develop", "main" ]
  pull_request:
    branches: [ "develop", "main" ]
  schedule:
    - cron: '0 3 * * 1'

jobs:
  snyk:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Install dependencies
        run: npm ci --legacy-peer-deps
      - name: Run Snyk to check for vulnerabilities
        uses: snyk/actions/node@master
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        with:
          command: test
          args: --severity-threshold=medium

  codeql:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: javascript
      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3

  dependency-review:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
      - name: Dependency Review
        uses: actions/dependency-review-action@v4

  sonarcloud:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@master
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          args: >
            -Dsonar.projectKey=${{ secrets.SONAR_PROJECT_KEY }}
            -Dsonar.organization=${{ secrets.SONAR_ORGANIZATION }}
            -Dsonar.qualitygate.wait=true

  docker-scout:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
      - name: Build Docker image
        run: docker build -t ${{ github.repository }}:pr-${{ github.run_id }} .
      - name: Docker Scout scan
        uses: docker/scout-action@v1
        with:
          image: ${{ github.repository }}:pr-${{ github.run_id }}
          only-severities: critical,high
          exit-code: true

  docker-push:
    runs-on: ubuntu-latest
    needs: [snyk, codeql, dependency-review, sonarcloud, docker-scout]
    if: ${{ needs.snyk.result == 'success' && needs.codeql.result == 'success' && (github.event_name != 'pull_request' || needs.dependency-review.result == 'success') && (github.event_name != 'pull_request' || needs.sonarcloud.result == 'success') && needs.docker-scout.result == 'success' }}
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and tag image
        run: |
          TAG=${{ github.sha }}
          docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/${{ github.repository }}:${TAG} .
          docker tag ${{ secrets.DOCKERHUB_USERNAME }}/${{ github.repository }}:${TAG} ${{ secrets.DOCKERHUB_USERNAME }}/${{ github.repository }}:latest

      - name: Push image
        run: |
          TAG=${{ github.sha }}
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/${{ github.repository }}:${TAG}
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/${{ github.repository }}:latest

